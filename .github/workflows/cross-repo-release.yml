name: Deprecated Cross Repo Release

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow has been deprecated."
          echo "Use .github/workflows/release.yml for tag-based releases in this repository."
